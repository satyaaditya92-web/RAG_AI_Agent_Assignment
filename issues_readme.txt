After changing the LLM_MODEL to "gpt-4o-mini", it is throwing the 403 error. Suggesting to upgrade your OpenAI account/credits to enable access to GPT-4 models.

Option 2: Enable gpt-4o-mini access for your project
Go to platform.openai.com
Navigate to Your Project → Settings → Limits
Check if gpt-4o-mini is listed under available models — if not, it may require a paid tier or a different project type
Alternatively, create a new project under an account with full model access

Got the above suggestion from the ChatGPT, but not able to implement. 